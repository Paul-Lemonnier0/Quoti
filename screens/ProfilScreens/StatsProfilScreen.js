import { StyleSheet, View } from "react-native"
import { NormalText } from "../../styles/StyledText";

export const StatProfilScreen = () => {
  return(
    <UsualScreen>
      <View>
        <NormalText text="Stats profil"/>
      </View>
    </UsualScreen>)
}